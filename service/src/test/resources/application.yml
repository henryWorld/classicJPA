region: common

server:
  port: 8200

spring:
  profiles:
    group:
      default:
        - "common"
        - "commonLiquibase"
        - "commonDbLegacyTest"
        - "commonTest"
        - "commonNoEnactor"
        - "service"
        - "serviceTest"

logging:
  level:
    com.specsavers.socrates.clinical: INFO
