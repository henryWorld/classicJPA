mutation ($sightTestId: ID!, $version: Int!){
  updatePrescribedRxNote(sightTestId: $sightTestId, version: $version, text: "PrescribedRx Notes") {
    prescribedRx {
      notes {
        text
        optomName
        date
      }
    }
  }
}
