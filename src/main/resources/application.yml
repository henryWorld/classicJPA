server:
  port: 8080

spring:
  profiles:
    group:
      default:
        - "common"
        - "commonDbLegacyAsMain"
      dev:
        - "common"
        - "commonDev"
        - "commonDbLegacyAsMain"
  datasource:
    url: jdbc:mysql://${DATABASE_SERVER}/soa8306?useSSL=true&requireSSL=false
